syntax = 'proto3';
import 'common/meta.proto';

package omni.base.proto.users.events;

message UserCreatedEvent {
  string id = 1;
  User user = 2;
  common.Meta meta = 3;
}

message User {
  string email = 1;
  string password = 2;
}