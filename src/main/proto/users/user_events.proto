syntax = 'proto3';
import 'common/meta.proto';

package omni.base.proto.users.events;

message Created {
  string id = 1;
  Status status = 2;
  common.Meta meta = 3;
}

enum Status {
  CREATED = 0;
}